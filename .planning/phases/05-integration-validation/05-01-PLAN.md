---
phase: 05-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fuzzer/tests/test_java_integration.py
  - fuzzer/tests/data/jacoco_sample.xml
autonomous: true
requirements:
  - INT-01
  - INT-02
  - VAL-05

must_haves:
  truths:
    - "CoverageRunner.run_java() parses JaCoCo XML and returns CoveredFunction list"
    - "CoverageRunner.run() dispatches to run_java() for JAVA language"
    - "CoverageBot stores Java function coverage in Redis CoverageMap"
  artifacts:
    - path: "fuzzer/tests/test_java_integration.py"
      provides: "Integration tests for Java coverage pipeline"
      min_lines: 100
    - path: "fuzzer/tests/data/jacoco_sample.xml"
      provides: "Realistic JaCoCo XML fixture for testing"
      contains: "<report"
  key_links:
    - from: "test_java_integration.py"
      to: "coverage_runner.py"
      via: "CoverageRunner.run_java() import and invocation"
      pattern: "CoverageRunner.*run_java"
    - from: "test_java_integration.py"
      to: "coverage_bot.py"
      via: "CoverageBot._submit_function_coverage() mock verification"
      pattern: "_submit_function_coverage"
---

<objective>
Create comprehensive integration tests for Java coverage pipeline to validate that Phase 4 infrastructure (JaCoCo integration in helper.py, CoverageRunner.run_java(), CoverageBot Redis submission) works end-to-end.

Purpose: Verify INT-01 (language dispatch), INT-02 (XML parsing), and VAL-05 (Redis population) with automated tests before manual deployment validation.

Output: test_java_integration.py with unit and integration tests, jacoco_sample.xml fixture, tests passing.
</objective>

<execution_context>
@/home/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/home/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-validation/05-RESEARCH.md

# Phase 4 outputs (what we're validating)
@.planning/phases/04-java-coverage-infrastructure/04-02-SUMMARY.md

# Existing test patterns to follow
@fuzzer/tests/test_coverage_runner.py
@fuzzer/tests/test_coverage_bot.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From fuzzer/src/buttercup/fuzzing_infra/coverage_runner.py:
```python
from buttercup.common.project_yaml import Language

class CoveredFunction:
    names: str
    total_lines: int
    covered_lines: int
    function_paths: list[str]

class CoverageRunner:
    def run(self, harness_name: str, corpus_dir: str) -> list[CoveredFunction] | None:
        """Dispatches to run_java() or run_c() based on ProjectYaml.unified_language"""

    def run_java(self, harness_name: str, corpus_dir: str) -> list[CoveredFunction] | None:
        """Parses JaCoCo XML from <build_dir>/dumps/<harness_name>.xml using BeautifulSoup"""
```

From fuzzer/src/buttercup/fuzzing_infra/coverage_bot.py:
```python
class CoverageBot:
    def _submit_function_coverage(
        self,
        func_coverage: list[CoveredFunction],
        harness_name: str,
        package_name: str,
        task_id: str,
    ) -> None:
        """Stores function coverage in Redis CoverageMap"""
```

From buttercup/common/project_yaml.py:
```python
class Language(Enum):
    C = "c"
    CPP = "c++"
    JAVA = "java"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create JaCoCo XML fixture</name>
  <files>fuzzer/tests/data/jacoco_sample.xml</files>
  <action>
Create the tests/data directory if it does not exist, then create a realistic JaCoCo XML fixture file.

The XML must match JaCoCo 0.8.11 output format with:
- Root `<report>` element with name attribute
- `<sessioninfo>` element with id, start, dump timestamps
- `<package>` element with name="com/example"
- `<class>` element with name="com/example/MyClass" and sourcefilename="MyClass.java"
- Multiple `<method>` elements with name, desc, and line attributes
- `<counter>` elements for INSTRUCTION, LINE, and BRANCH with missed/covered counts

Include at least 3 methods with varying coverage (some fully covered, some partially, one uncovered) to exercise different code paths in run_java().

Example counter values:
- Method 1: LINE missed=0 covered=5 (fully covered)
- Method 2: LINE missed=2 covered=3 (partially covered)
- Method 3: LINE missed=4 covered=0 (uncovered)
  </action>
  <verify>
    <automated>test -f /home/andrew/post/buttercup-seed-gen/fuzzer/tests/data/jacoco_sample.xml && grep -q "<report" /home/andrew/post/buttercup-seed-gen/fuzzer/tests/data/jacoco_sample.xml && echo "PASS"</automated>
  </verify>
  <done>JaCoCo XML fixture exists with valid structure matching JaCoCo 0.8.11 output</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Java integration tests</name>
  <files>fuzzer/tests/test_java_integration.py</files>
  <behavior>
    - test_run_java_parses_jacoco_xml: Given valid JaCoCo XML at expected path, run_java() returns list of CoveredFunction with correct names, total_lines, covered_lines
    - test_run_java_handles_missing_xml: Given no XML file at path, run_java() logs error and returns None
    - test_run_dispatches_to_java_for_java_language: Given Language.JAVA, CoverageRunner.run() calls run_java() not run_c()
    - test_run_dispatches_to_c_for_c_language: Given Language.C, CoverageRunner.run() calls run_c() not run_java()
    - test_coverage_bot_submits_java_coverage_to_redis: Given Java CoveredFunction list, CoverageBot._submit_function_coverage() stores in CoverageMap and can be retrieved
  </behavior>
  <action>
Create test_java_integration.py following the patterns in test_coverage_runner.py and test_coverage_bot.py:

1. Import required modules: pytest, MagicMock, patch, tempfile, Path
2. Import from coverage_runner: CoverageRunner, CoveredFunction
3. Import from coverage_bot: CoverageBot
4. Import from common: Language, CoverageMap, Redis

5. Create fixtures:
   - `mock_java_tool`: MagicMock with project_name, get_build_dir returning temp path with dumps/<harness>.xml
   - `jacoco_xml_content`: Load from tests/data/jacoco_sample.xml
   - `redis_client`: Redis(host="localhost", port=6379, db=13) with flushdb in teardown (copy from test_coverage_bot.py)

6. Test: test_run_java_parses_jacoco_xml
   - Create temp build_dir with dumps/<harness>.xml containing jacoco_xml_content
   - Mock tool.run_coverage() to return success
   - Mock ProjectYaml to return Language.JAVA
   - Call runner.run_java("JavaHarness", "/tmp/corpus")
   - Assert returns list with CoveredFunction items
   - Assert function names match XML method names
   - Assert total_lines = missed + covered
   - Assert covered_lines = covered count

7. Test: test_run_java_handles_missing_xml
   - Create temp build_dir with NO dumps/<harness>.xml
   - Mock tool.run_coverage() to return success
   - Call runner.run_java("MissingHarness", "/tmp/corpus")
   - Assert returns None (not exception)

8. Test: test_run_dispatches_to_java_for_java_language
   - Mock ProjectYaml to return Language.JAVA
   - Patch run_java and run_c
   - Call runner.run("JavaHarness", "/tmp/corpus")
   - Assert run_java was called, run_c was NOT called

9. Test: test_run_dispatches_to_c_for_c_language
   - Mock ProjectYaml to return Language.C
   - Patch run_java and run_c
   - Call runner.run("CHarness", "/tmp/corpus")
   - Assert run_c was called, run_java was NOT called

10. Test: test_coverage_bot_submits_java_coverage_to_redis (requires running Redis)
    - Create CoveredFunction list with Java-style method names (com.example.MyClass.myMethod)
    - Create CoverageBot with redis_client
    - Call bot._submit_function_coverage(func_coverage, "JavaHarness", "java_pkg", "task123")
    - Create CoverageMap with same params
    - Call coverage_map.get_function_coverage() to retrieve
    - Assert retrieved FunctionCoverage matches input

Use @pytest.mark.skipif for Redis test if Redis not available.
  </action>
  <verify>
    <automated>cd /home/andrew/post/buttercup-seed-gen/fuzzer && uv run pytest tests/test_java_integration.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 tests pass: XML parsing, missing file handling, language dispatch (both directions), Redis submission</done>
</task>

<task type="auto">
  <name>Task 3: Verify integration with existing test suite</name>
  <files>fuzzer/tests/test_java_integration.py</files>
  <action>
Run the full fuzzer test suite to verify new tests integrate without breaking existing tests.

If any tests fail, investigate and fix. Common issues:
- Import errors: ensure all imports use correct paths
- Redis connection: skip Redis tests if Redis not running
- Mock conflicts: use fresh mocks per test

Update test file if necessary to resolve any issues.
  </action>
  <verify>
    <automated>cd /home/andrew/post/buttercup-seed-gen/fuzzer && uv run pytest tests/test_coverage_runner.py tests/test_coverage_bot.py tests/test_java_integration.py -x 2>&1 | tail -20</automated>
  </verify>
  <done>All coverage-related tests pass together without conflicts</done>
</task>

</tasks>

<verification>
1. JaCoCo XML fixture exists and has valid structure
2. test_java_integration.py exists with 5 test functions
3. All tests pass in isolation: `pytest tests/test_java_integration.py -v`
4. Tests integrate with existing suite: `pytest tests/test_coverage*.py tests/test_java_integration.py -x`
</verification>

<success_criteria>
- JaCoCo XML fixture at fuzzer/tests/data/jacoco_sample.xml
- test_java_integration.py with passing tests for:
  - INT-01: Language dispatch (run() calls correct method based on Language)
  - INT-02: XML parsing (run_java() correctly parses JaCoCo XML)
  - VAL-05: Redis submission (CoverageBot stores Java coverage in CoverageMap)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-validation/05-01-SUMMARY.md`
</output>
