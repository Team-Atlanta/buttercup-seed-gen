---
phase: 05-integration-validation
task: 1
total_tasks: 1
status: blocked
last_updated: 2026-03-23T16:10:11.491Z
---

<current_state>
Phase 5 Plan 01 (logging) complete. Plan 02 (deployment validation) is a human checkpoint that's blocked because seed-init fails to find the Java harness.

Root cause identified: `find_harness.py` searches `container_src_dir/` for Java harnesses, but `builder-codequery.sh` only copies target source (`/src`) there — not the fuzz-proj where `OssFuzz1.java` lives.
</current_state>

<completed_work>

- Phase 4: Java Coverage Infrastructure - COMPLETE (2 plans, all verified)
  - 04-01: Added java/jvm to crs.yaml, installed JaCoCo JARs
  - 04-02: Added logging to helper.py for language detection, dispatch, JaCoCo execution

- Phase 5 Plan 01: Add logging to helper.py - COMPLETE
  - 47 logging statements with `[COVERAGE]` prefix
  - Language detection, Java/C++ dispatch, JaCoCo execution logging
  - Commit: 64a1fee

</completed_work>

<remaining_work>

- Phase 5 Plan 02: Deployment validation checkpoint - BLOCKED
  - Cannot validate Java coverage because seed-init fails with "No harness found"
  - Need to fix builder-codequery.sh first

- Gap closure needed: Fix builder-codequery.sh to copy fuzz-proj

</remaining_work>

<decisions_made>

- Replanned Phase 5 to add logging instead of integration tests (user preference)
- Confirmed find_harness.py implementation is correct - issue is missing fuzz-proj in container_src_dir
- Original buttercup uses docker cp /src which includes both target and fuzz-proj; oss-crs-6 splits these

</decisions_made>

<blockers>

- **Harness discovery fails for Java targets**: `find_jazzer_harnesses()` searches `container_src_dir/` but Java harness files are in fuzz-proj (`/OSS_CRS_FUZZ_PROJ`), not target source (`/src`)

  Fix: Update `builder-codequery.sh` to also copy fuzz-proj:
  ```bash
  if [ -d "/OSS_CRS_FUZZ_PROJ" ]; then
      cp -r /OSS_CRS_FUZZ_PROJ "$CONTAINER_SRC_DIR/"
  fi
  ```

</blockers>

<context>
v1.1 Java Support milestone. Phase 4 infrastructure complete and verified. Phase 5 blocked on seed-gen harness discovery.

The issue is that oss-crs-6 uses libCRS which splits source into:
- `target-source` (/OSS_CRS_TARGET_SOURCE) - library being fuzzed
- `fuzz-proj` (/OSS_CRS_FUZZ_PROJ) - harness + build files

But builder-codequery.sh only copies target source, so find_harness can't find Java harnesses.
</context>

<next_action>
Fix builder-codequery.sh to copy fuzz-proj to container_src_dir/:

1. Edit `oss-crs/bin/builder-codequery.sh`
2. Add copy of `/OSS_CRS_FUZZ_PROJ` after existing source copy
3. Re-run oss-crs build-target and run to validate fix
4. Complete Phase 5 Plan 02 checkpoint
</next_action>
