---
phase: 03-validation-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seed-gen/test/test_seed_gen_bot.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "All seed-gen unit tests pass after code cleanup"
    - "Task sampling returns only SEED_INIT and SEED_EXPLORE"
    - "No references to crash infrastructure in seed-gen codebase"
  artifacts:
    - path: "seed-gen/test/test_seed_gen_bot.py"
      provides: "Task sampling tests"
      contains: "test_sample_task_never_returns_vuln_discovery"
  key_links:
    - from: "seed_gen_bot.py"
      to: "task.py"
      via: "TaskName enum"
      pattern: "TaskName\\.(SEED_INIT|SEED_EXPLORE)"
---

<objective>
Verify seed-gen unit tests pass after Phase 2 code cleanup

Purpose: Confirm code changes didn't break seed generation logic before attempting integration tests
Output: Green test suite with validation that task sampling and seed generation work correctly
</objective>

<execution_context>
@/home/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/home/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-code-cleanup/02-01-SUMMARY.md
@.planning/phases/02-code-cleanup/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run seed-gen unit test suite</name>
  <files>seed-gen/</files>
  <action>
    Run the full seed-gen test suite to verify all code changes from Phase 2 are working correctly.

    Commands to run:
    ```bash
    cd /home/andrew/post/buttercup-bugfind/seed-gen && uv run pytest -v
    ```

    Expected outcome:
    - All tests pass (17+ tests based on Phase 2 summary)
    - Task sampling tests confirm no vuln-discovery returned
    - No import errors from deleted modules

    If tests fail:
    - Document which tests fail and why
    - If failures are infrastructure-dependent (Redis, CodeQuery), note them as expected
    - Only blocking failures are logic errors in seed generation

    Note: Some tests may be skipped due to infrastructure dependencies (test_find_harness.py, test_task_counter.py) - this is expected per Phase 2 summary.
  </action>
  <verify>
    <automated>cd /home/andrew/post/buttercup-bugfind/seed-gen && uv run pytest --ignore=test/test_find_harness.py --ignore=test/test_task_counter.py -v</automated>
  </verify>
  <done>All seed-gen unit tests pass (excluding infrastructure-dependent tests)</done>
</task>

<task type="auto">
  <name>Task 2: Verify no dead code references</name>
  <files>seed-gen/src/</files>
  <action>
    Verify that no references to removed infrastructure remain in the seed-gen codebase.

    Search patterns to verify are absent:
    1. Crash infrastructure: `crash_queue`, `crash_set`, `CrashSet`, `CrashDir`
    2. Vuln-discovery: `VULN_DISCOVERY`, `VulnDiscovery`, `CrashSubmit`, `SARIFStore`, `ReproduceMultiple`
    3. POV submission: `pov`, `POV` (in submission context)

    Commands to run:
    ```bash
    cd /home/andrew/post/buttercup-bugfind/seed-gen
    rg "crash_queue|crash_set|CrashSet|CrashDir" src/
    rg "VULN_DISCOVERY|VulnDiscovery|CrashSubmit|SARIFStore|ReproduceMultiple" src/
    ```

    Expected: No matches found (exit code 1 from rg)

    If matches found:
    - Document location and context
    - If benign (comments, strings), note as acceptable
    - If active code references, this is a Phase 2 gap requiring fix
  </action>
  <verify>
    <automated>cd /home/andrew/post/buttercup-bugfind/seed-gen && rg "crash_queue|crash_set|CrashSet|CrashDir|VULN_DISCOVERY|VulnDiscovery|CrashSubmit|SARIFStore|ReproduceMultiple" src/ && echo "FAIL: Dead code found" || echo "PASS: No dead code references"</automated>
  </verify>
  <done>No references to removed infrastructure in seed-gen/src/</done>
</task>

</tasks>

<verification>
Phase verification for unit test plan:

1. Test suite passes: `cd seed-gen && uv run pytest` shows green
2. No dead code: `rg` searches return no matches
3. Lint passes: `cd seed-gen && uv run ruff check src/` shows no errors
</verification>

<success_criteria>
- All seed-gen unit tests pass (17+ tests)
- No references to crash infrastructure in codebase
- No references to vuln-discovery in codebase
- Code is lint-clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-documentation/03-01-SUMMARY.md`
</output>
