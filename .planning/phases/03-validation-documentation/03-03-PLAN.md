---
phase: 03-validation-documentation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - oss-crs/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "README documents 4-service standalone architecture"
    - "No fuzzer-bot references in README"
    - "Deployment instructions use crs-compose"
    - "Data flow diagram shows seed-gen -> corpus -> libCRS path"
  artifacts:
    - path: "oss-crs/README.md"
      provides: "Standalone architecture documentation"
      min_lines: 100
      contains: "4 services"
  key_links:
    - from: "README.md"
      to: "crs.yaml"
      via: "service list"
      pattern: "redis.*orchestrator.*coverage.*seed"
---

<objective>
Document the standalone seed-gen architecture in oss-crs README

Purpose: Codify the validated architecture so future developers understand the seed-only deployment pattern
Output: Updated README.md with clear documentation of 4-service architecture, no fuzzer references
</objective>

<execution_context>
@/home/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/home/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-validation-documentation/03-RESEARCH.md
@.claude/skills/run-oss-crs/SKILL.md
</context>

<interfaces>
<!-- Key documentation patterns from research -->

From .planning/phases/03-validation-documentation/03-RESEARCH.md:
```markdown
# Buttercup Seed-Gen Standalone

## Overview
Slimmed-down seed generation service without fuzzing infrastructure.
Generates targeted test inputs and submits directly to competition API.

## Architecture

### Services (4 total)
- **redis**: Message broker for service coordination
- **orchestrator**: Populates Redis with build artifacts
- **coverage-bot**: Measures coverage metrics
- **seed-gen**: Generates seeds via LLM analysis

**Removed:** fuzzer-bot (no longer needed for seed-only workflow)

### Data Flow
1. libCRS downloads build artifacts (task, task-coverage, cqdb)
2. Orchestrator registers builds in Redis (BuildMap, HarnessWeights)
3. Coverage-bot measures coverage -> Redis CoverageMap
4. Seed-gen generates seeds -> /artifacts/corpus/
5. libCRS watcher submits seeds -> Competition API
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Read existing README and oss-crs structure</name>
  <files>oss-crs/README.md, oss-crs/crs.yaml</files>
  <action>
    Read the existing oss-crs documentation to understand current state:

    1. Read oss-crs/README.md (if exists) to see current documentation
    2. Read oss-crs/crs.yaml to confirm service configuration
    3. Note what needs to be updated or created

    If README.md exists:
    - Identify fuzzer-bot references to remove
    - Identify sections to update for 4-service architecture
    - Preserve any useful content (development commands, etc.)

    If README.md doesn't exist:
    - Create new file based on research template
    - Include all sections from RESEARCH.md pattern
  </action>
  <verify>
    <automated>ls -la /home/andrew/post/buttercup-bugfind/oss-crs/README.md /home/andrew/post/buttercup-bugfind/oss-crs/crs.yaml 2>&1</automated>
  </verify>
  <done>Current README state documented, update strategy determined</done>
</task>

<task type="auto">
  <name>Task 2: Create/update standalone architecture README</name>
  <files>oss-crs/README.md</files>
  <action>
    Create or update oss-crs/README.md with standalone architecture documentation.

    **Required sections:**

    1. **Overview** - Slimmed-down seed generation service description
       - Purpose: Generate targeted test inputs and submit to competition API
       - NOT a fuzzer: No vulnerability discovery or POV generation

    2. **Architecture** - 4-service diagram
       - redis: Message broker
       - orchestrator: Redis population from build artifacts
       - coverage-bot: Coverage measurement
       - seed-gen: LLM-powered seed generation
       - Explicit note: "fuzzer-bot removed"

    3. **Data Flow** - From build artifacts to seed submission
       - libCRS downloads -> Orchestrator -> Redis
       - Seed-gen generates -> /artifacts/corpus/
       - libCRS watcher -> Competition API

    4. **Deployment** - crs-compose commands
       - prepare, build-target, run commands
       - Reference to buttercup-compose.yaml configs
       - No-LLM option for testing

    5. **Configuration Files**
       - crs.yaml: Service definitions
       - buttercup_entrypoint: Service routing
       - buttercup-runner.Dockerfile: Container image

    6. **Development**
       - Running tests: cd seed-gen && uv run pytest
       - Linting: make lint-component COMPONENT=seed-gen

    7. **Migration Notes** (from full Buttercup)
       - What was removed: fuzzer-bot, vuln-discovery, POV submission
       - What remains: seed-init, seed-explore, corpus management

    **Content rules:**
    - NO fuzzer-bot references (except in "removed" context)
    - NO vulnerability discovery references (except in "removed" context)
    - Focus on seed generation workflow
    - Use crs-compose commands (not legacy oss-bugfind-crs)
  </action>
  <verify>
    <automated>cat /home/andrew/post/buttercup-bugfind/oss-crs/README.md | head -100</automated>
  </verify>
  <done>README.md documents 4-service standalone architecture</done>
</task>

<task type="auto">
  <name>Task 3: Verify README has no active fuzzer references</name>
  <files>oss-crs/README.md</files>
  <action>
    Verify the README does not contain active fuzzer-bot references.

    Allowed references (in "removed" or "migration" context):
    - "fuzzer-bot removed"
    - "no longer includes fuzzer"
    - Migration notes explaining what was removed

    NOT allowed (active capability claims):
    - "fuzzer-bot service"
    - "vulnerability discovery"
    - "POV generation"
    - Instructions to run fuzzer

    Search command:
    ```bash
    rg -i "fuzzer|vuln|pov" /home/andrew/post/buttercup-bugfind/oss-crs/README.md
    ```

    Review each match:
    - If in "removed" context: OK
    - If implying active capability: Fix
  </action>
  <verify>
    <automated>rg -i "fuzzer-bot service|vulnerability discovery|POV generation" /home/andrew/post/buttercup-bugfind/oss-crs/README.md && echo "FAIL: Active fuzzer references found" || echo "PASS: No active fuzzer references"</automated>
  </verify>
  <done>README contains no active fuzzer-bot capability claims</done>
</task>

</tasks>

<verification>
Documentation verification:

1. README exists and is substantial (100+ lines)
2. Contains "4 services" or equivalent
3. Contains seed-gen workflow description
4. Contains crs-compose deployment commands
5. Does NOT claim fuzzer-bot capability
6. Does NOT claim vulnerability discovery
7. Does NOT claim POV generation
</verification>

<success_criteria>
- oss-crs/README.md exists with 100+ lines
- Documents 4-service architecture (redis, orchestrator, coverage-bot, seed-gen)
- Includes crs-compose deployment instructions
- Contains data flow diagram (text or ASCII)
- No active fuzzer-bot references
- Migration notes explain what was removed
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-documentation/03-03-SUMMARY.md`
</output>
